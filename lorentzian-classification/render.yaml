services:
  - type: web
    name: auto-trade
    env: python
    buildCommand: pip install fastapi uvicorn aiohttp pydantic pydantic-settings python-dotenv prometheus-client redis pytz holidays
    startCommand: python python_bridge.py
    envVars:
      - key: OANDA_ACCOUNT_ID
        sync: false
      - key: OANDA_API_TOKEN
        sync: false
